"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// packages/ui/src/utils/use-deprioritized-initial-focus.ts
var use_deprioritized_initial_focus_exports = {};
__export(use_deprioritized_initial_focus_exports, {
  useDeprioritizedInitialFocus: () => useDeprioritizedInitialFocus
});
module.exports = __toCommonJS(use_deprioritized_initial_focus_exports);
var import_element = require("@wordpress/element");
var import_tabbable = require("tabbable");
var getTabbableOptions = () => ({
  getShadowRoot: true,
  displayCheck: typeof ResizeObserver === "function" && ResizeObserver.toString().includes("[native code]") ? "full" : "none"
});
function useDeprioritizedInitialFocus({
  initialFocus,
  deprioritizedAttribute
}) {
  const popupRef = (0, import_element.useRef)(null);
  const resolvedInitialFocus = (0, import_element.useMemo)(() => {
    if (initialFocus !== void 0 && initialFocus !== true) {
      return initialFocus;
    }
    return (interactionType) => {
      if (interactionType === "touch") {
        return popupRef.current ?? true;
      }
      const popup = popupRef.current;
      if (!popup) {
        return true;
      }
      const tabbables = (0, import_tabbable.tabbable)(popup, getTabbableOptions());
      for (const el of tabbables) {
        if (el instanceof HTMLElement && !el.hasAttribute(deprioritizedAttribute)) {
          return el;
        }
      }
      return true;
    };
  }, [initialFocus, deprioritizedAttribute]);
  return { resolvedInitialFocus, popupRef };
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  useDeprioritizedInitialFocus
});
//# sourceMappingURL=use-deprioritized-initial-focus.cjs.map
